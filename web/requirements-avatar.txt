# Binary Avatar System Requirements
# Install with: pip install -r requirements-avatar.txt

# WebSocket server
websockets>=12.0

# Audio processing
numpy>=1.24.0
soundfile>=0.12.0

# TTS engines (choose one or both)
# High fidelity TTS
TTS>=0.22.0  # Coqui TTS

# Piper TTS — used by kernel/piper_viseme_server.py for low-latency
# phoneme-aligned audio synthesis. The PyPI package is `piper-tts`,
# but it imports as `piper`. Do not confuse with the unrelated `piper`
# package on PyPI (a process supervisor).
piper-tts>=1.4.0

# System monitoring
psutil>=5.9.0
