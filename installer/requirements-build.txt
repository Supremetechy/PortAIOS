# Build-time dependencies for packaging PortAIOS.
# These are installed in a clean venv by each OS build script.

# Runtime deps (must match what the app imports)
eel>=0.16.0
bottle>=0.12
bottle-websocket>=0.2.9
gevent>=23.0
gevent-websocket>=0.10.1
websockets>=12.0
numpy>=1.24
psutil>=5.9
GPUtil>=1.4
soundfile>=0.12
Pillow>=10.0

# Packaging toolchain
pyinstaller>=6.3.0
