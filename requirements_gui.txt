# =============================================================================
# PortAIOS — Desktop GUI Requirements
# =============================================================================
# Extends the core requirements with GUI frameworks, audio hardware, and
# full TTS support needed for the native desktop application.
# Install: pip install -r requirements_gui.txt

# Core server deps
-r requirements.txt

# ── Desktop GUI frameworks ────────────────────────────────────────────────────
# PyQt6 for any native widgets / fallback display paths
PyQt6>=6.4.0

# IMPORTANT: install `pywebview`, NOT `webview` — the latter is a broken
# unmaintained PyPI package. pywebview exposes an importable module named
# `webview`, which is what the kernel code imports.
pywebview>=5.0

# ── Audio hardware (desktop only) ────────────────────────────────────────────
# Requires PortAudio system library:
#   macOS:  brew install portaudio
#   Ubuntu: apt-get install portaudio19-dev
pyaudio>=0.2.13

# ── TTS engines (large download — comment out what you don't need) ────────────
TTS>=0.22.0          # Coqui TTS — high-fidelity neural voices
piper-tts>=1.4.0     # Piper TTS — low-latency, phoneme-aligned lip-sync
