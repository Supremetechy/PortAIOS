# =============================================================================
# PortAIOS — Core Requirements
# =============================================================================
# Covers all server-mode and kernel functionality.
# For desktop GUI extras:   pip install -r requirements_gui.txt
# For Docker / headless:    pip install -r requirements-server.txt

# ── Web / async backend ───────────────────────────────────────────────────────
eel>=0.16.0
bottle>=0.12
bottle-websocket>=0.2.9
gevent>=23.9
gevent-websocket>=0.10.1
websockets>=12.0

# ── System / hardware monitoring ─────────────────────────────────────────────
psutil>=5.9.0
GPUtil>=1.4.0

# ── Data / numerics ──────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ── Image / 3D / avatar ──────────────────────────────────────────────────────
Pillow>=10.0.0
trimesh>=4.0.0
soundfile>=0.12.0

# ── Voice recognition (browser fallback works without these) ─────────────────
SpeechRecognition>=3.10.0

# ── DeepGram Voice Agent (STT + LLM + TTS unified pipeline) ──────────────────
deepgram-sdk>=3.0.0

# ── Computer vision & gesture ────────────────────────────────────────────────
# opencv-python pulls in display libs; use headless variant in Docker.
# The requirements-server.txt file overrides this with opencv-python-headless.
opencv-python>=4.8.0
mediapipe>=0.10.0

# ── Cloud browser automation ─────────────────────────────────────────────────
browserbase>=0.1.0
playwright>=1.40.0

# ── AI / ML (optional — uncomment to enable) ─────────────────────────────────
# llama-cpp-python>=0.2.0   # local GGUF inference
# chromadb>=0.4.0            # vector DB for RAG

# ── TTS engines (optional — large download) ──────────────────────────────────
# TTS>=0.22.0                # Coqui TTS (high-fidelity)
# piper-tts>=1.4.0           # Piper TTS (low-latency, phoneme-aligned)
